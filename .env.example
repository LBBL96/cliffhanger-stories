# AI Provider Configuration
# Choose either 'openai' or 'anthropic'
# Note: Command-line flag takes priority over this setting
# Usage: python app.py --provider openai  (default)
#        python app.py --provider claude   (uses Anthropic)
AI_PROVIDER=openai

# OpenAI Configuration (if using OpenAI)
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o-2024-11-20

# Anthropic Configuration (if using Anthropic/Claude)
ANTHROPIC_API_KEY=your_anthropic_api_key_here
ANTHROPIC_MODEL=claude-opus-4-20250514

# Flask Secret Key
SECRET_KEY=your-secret-key-for-sessions-change-in-production
